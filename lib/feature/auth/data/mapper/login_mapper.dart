class LoginMapper {
  // static UsersEntity mapToLocal(LoginResponse response) {
  //   return UsersEntity(
  //       response.user.id,
  //       response.user.fullName,
  //       response.user.email,
  //       response.user.phoneNumber,
  //       response.user.firstName,
  //       response.user.lastName,
  //       response.user.role.id,
  //       response.user.role.name,
  //       response.buName,
  //       response.user.parentUserId);
  // }
}
